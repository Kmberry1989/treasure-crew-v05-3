{
  "name": "treasure-crew-v1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:scene": "node scripts/build-animation-catalog.mjs && node scripts/build-scene-runtime.mjs",
    "predev": "npm run build:scene",
    "dev": "node server.js",
    "prestart": "npm run build:scene",
    "start": "node server.js"
  },
  "dependencies": {
    "three": "^0.176.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.4"
  },
  "engines": {
    "node": ">=20"
  }
}
